image: registry.gitlab.com/razd-cli/razd-debian-slim:latest


build:
  script:
    - echo "=== Verify installation ==="
    - razd --version
    
    - razd install
    
    - echo "=== Razd build ==="
    - razd build